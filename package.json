{
  "name": "imgur-send-bot",
  "version": "1.0.0",
  "description": "Telegram → Imgur mirror bot running on Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
